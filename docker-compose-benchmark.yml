version: "3"
services:
  benchmark:
    build: .
    volumes:
      - ./configs/localdocker:/configs
    command: ["benchmark", "--config", "/configs/benchmark/config.toml", "--delay", "15", "-c", "2", "-d", "15"]
